# --- Cœur de l'application ---
streamlit>=1.36
SQLAlchemy>=2.0

# --- IA ---
google-genai>=1.0  # SDK unifié, remplace l'ancien google-generativeai déprécié
openai>=1.0        # SDK OpenAI pour DeepSeek-V4-Pro et hybride

# --- Sécurité ---
cryptography>=42.0  # Fernet pour le chiffrement de la clé API Gemini

# --- Étapes ultérieures (déjà déclarées pour limiter les surprises d'install) ---
pdfplumber>=0.11
PyMuPDF>=1.24
plotly>=5.20
icalendar>=5.0
reportlab>=4.0
pandas>=2.0

# --- Image (utilisé par modules/import_externe.py via PIL.Image) ---
Pillow>=10.0
audio-recorder-streamlit